[project]
name = "baton-labs"
version = "0.1.0"
description = ""
authors = [
    {name = "Jamon Douglas",email = "terrildouglas@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "starlette (>=0.46.1,<0.47.0)",
    "uvicorn (>=0.34.0,<0.35.0)",
    "ijson (>=3.3.0,<4.0.0)"
]

[tool.poetry]
packages = [{include = "my_first_app", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
